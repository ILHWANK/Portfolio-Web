---
const nav = [
    { label: "홈", href: "/" },
    { label: "OZ-Rewrite", href: "/projects" },
    { label: "편지와 마녀의 아이(LWC)", href: "/about" }
];
---
<header class="header">
    <div class="container header__inner">
        <a class="brand" href="/">김일환</a>
        <nav class="nav">
            {nav.map((n) => (
                    <a class="nav__link" href={n.href}>{n.label}</a>
            ))}
        </nav>
    </div>
</header>
